# 기여 가이드

## 기본 흐름

`main`은 검증된 안정 버전이고 `dev`는 기본 통합 개발 브랜치입니다. 두 브랜치에는 직접 커밋하거나 푸시하지 않습니다.

1. 작업을 설명하는 Issue를 먼저 확인하거나 만듭니다.
2. 최신 `dev`에서 Issue 전용 작업 브랜치를 만듭니다.
3. 한 브랜치에는 하나의 목적만 담습니다.
4. 실제 검증 결과를 확인한 뒤 `dev`를 대상으로 Pull Request를 엽니다.
5. 최소 한 명의 다른 팀원이 리뷰하고 필요한 CI가 통과한 뒤 병합합니다.
6. 안정 버전 반영은 `dev`에서 `main`으로 향하는 별도 Pull Request로 진행합니다.

브랜치 예시:

- `feature/android-favorites`
- `feature/api-benefits`
- `fix/android-map-permission`
- `data/seoul-cafe-verification`
- `docs/local-setup`
- `chore/collaboration-foundation`

브랜치 접두사는 `feature`, `fix`, `data`, `docs`, `chore`를 기본으로 사용합니다. 다른 유형이 필요하면 Issue에서 먼저 합의합니다.

커밋 메시지는 `영역: 변경 요약` 형식을 권장합니다.

```text
android: 현재 위치 권한 흐름 추가
data: 마포구 카페 혜택 출처 갱신
docs: 네이버 지도 키 설정 보완
```

## 작업 범위와 검토

- Issue의 목표, 작업 범위, 제외 범위와 완료 조건을 지킵니다.
- 요청하지 않은 대규모 리팩터링이나 아키텍처 변경을 함께 넣지 않습니다.
- 새 dependency, 데이터 스키마, DB Migration, 인증 또는 배포 변경은 이유와 위험을 PR에 기록하고 담당자 검토를 받습니다.
- AI가 생성한 변경도 작성자가 diff와 동작을 직접 검토하고 책임집니다.
- 테스트 결과는 실제 실행한 명령만 기록하며 실행하지 못한 검증은 이유와 함께 남깁니다.
- 팀에서 결정되지 않은 사항은 임의로 확정하지 않고 `Pending` 또는 후속 Issue로 남깁니다.

## Pull Request 체크리스트

- 관련 Issue와 작업하지 않은 범위를 기록했는가?
- 변경 목적과 사용자 영향을 설명했는가?
- Android 변경이면 lint·test·assemble을 실행했는가?
- 실행하지 못한 검증과 이유를 기록했는가?
- 새 환경변수는 예제 파일과 문서에만 추가했는가?
- 로그, 스크린샷, CSV에 비밀정보가 포함되지 않았는가?
- 혜택 데이터 변경에는 근거 출처와 확인일이 있는가?
- AI 사용 범위와 사람이 직접 검토한 부분을 기록했는가?

## 데이터 품질 원칙

- 근거 없는 혜택을 생성하지 않습니다.
- 정확하지 않은 필드는 추측하지 않고 비워둡니다.
- 종료·변경 가능성이 있는 정보에는 `확인 필요` 상태를 사용합니다.
- 동일 업체는 정규화된 업체명과 주소를 기준으로 중복을 점검합니다.

세부 기준은 `docs/data-policy.md`를 따릅니다.
